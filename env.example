# ------------------------
# Next.js frontend
# ------------------------

# Basis-URL van je eigen site (nodig zodat fetch in SSR werkt)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Externe API endpoint (straks .NET backend) — leeg laten voor nu
NEXT_PUBLIC_API_BASE=

# Secret voor Next.js on-demand revalidate endpoint
REVALIDATE_SECRET=changeme-secret

# ------------------------
# ASP.NET Core backend (komt later)
# ------------------------

# MySQL connectiestring (voorbeeld)
DB_CONNECTION=Server=localhost;Port=3306;Database=shop;User=root;Password=changeme;

# Redis connectie (cache)
REDIS_CONNECTION=localhost:6379

# ASP.NET Core environment
ASPNETCORE_ENVIRONMENT=Development